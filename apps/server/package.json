{
  "name": "migpt-server",
  "version": "1.4.0",
  "type": "module",
  "scripts": {
    "clean": "rm -rf dist",
    "build": "tsc -p tsconfig.build.json",
    "dev": "tsc",
    "dev:watch": "tsc -w",
    "start": "node dist/index.js --open --user a --pwd b"
  },
  "bin": {
    "migpt-server": "dist/index.js"
  },
  "files": [
    "dist"
  ],
  "dependencies": {
    "@migptgui/server": "^1.1.0",
    "@migptgui/gui": "^1.5.0",
    "commander": "^12.1.0"
  },
  "devDependencies": {
    "nodemon": "^3.1.4",
    "@types/node": "^20.14.7",
    "typescript": "^5.5.2"
  }
}
