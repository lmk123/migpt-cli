{
  "name": "migpt-server",
  "version": "1.11.0",
  "type": "module",
  "scripts": {
    "clean": "rm -rf dist",
    "build": "tsc -p tsconfig.build.json",
    "dev": "tsc",
    "dev:watch": "tsc -w",
    "start": "node dist/index.js --open --user a --pwd b"
  },
  "bin": {
    "migpt-server": "dist/index.js"
  },
  "files": [
    "dist"
  ],
  "dependencies": {
    "@migptgui/gui": "^1.11.0",
    "@migptgui/server": "^1.11.0",
    "commander": "^12.1.0"
  },
  "devDependencies": {
    "@types/node": "^22.7.3",
    "typescript": "^5.6.2"
  }
}
