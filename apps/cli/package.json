{
  "name": "migpt-cli",
  "version": "1.1.0",
  "bin": {
    "migpt": "dist/index.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "clean": "rm -rf dist",
    "build": "tsc -p tsconfig.build.json -m NodeNext",
    "dev": "tsc -p tsconfig.build.json -m NodeNext -w",
    "prepublishOnly": "npm run clean && npm run build",
    "publish": "npm publish --access public",
    "start": "node dist/index.js"
  },
  "devDependencies": {
    "@hcfy/js-to-mjs": "^1.1.0",
    "@types/express": "^4.17.21",
    "@types/node": "^20.14.6",
    "typescript": "^5.4.5"
  },
  "dependencies": {
    "commander": "^12.1.0",
    "express": "^4.19.2",
    "mi-gpt": "^4.0.0",
    "open": "^8.4.2"
  }
}
